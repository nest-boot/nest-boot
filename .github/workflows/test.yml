name: lint

on: workflow_call

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Download install artifact
        uses: actions/download-artifact@v3
        with:
          name: install-${{ github.run_id }}

      - name: Test
        run: pnpm run test
