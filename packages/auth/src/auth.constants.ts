/**
 * Injection token for the Auth service.
 */
export const AUTH_TOKEN = Symbol("AUTH");

/**
 * Metadata key for marking a route as public.
 */
export const IS_PUBLIC_KEY = Symbol("IS_PUBLIC_KEY");
