import {
  EntityService,
  {{ "SearchService," if searchable }}
  TransactionalConnection,
} from "@nest-boot/database";
import { Injectable } from "@nestjs/common";

import { {{ name | camelize }} } from "../../core/entities/{{ name | underscore | dasherize }}.entity";

@Injectable()
export class {{ name | camelize }}Service extends EntityService<{{ name | camelize }}> {
  protected readonly target = {{ name | camelize }};

  constructor(
    protected readonly connection: TransactionalConnection,
    {{ "protected readonly searchService: SearchService" if searchable }}
  ) {
    super();
  }
}
